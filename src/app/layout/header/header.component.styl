@import "../../../styles/color-palette.styl"

.container
  width: 100%
  height: 97px
  background-color banner
  display flex
  align-items center
  .row
    width 100%
    margin 0 56px
    display flex
    justify-content space-between
    align-items center
    span
      font-size 20px
      color #222
    .logout
      text-decoration underline
      &:hover
        cursor pointer
